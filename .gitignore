# Rust
/target

# Node / React / Vite / Next.js
node_modules/
dist/
*.local
.vite/
.next/
.next
out/
.vercel

# Python server
server/.env
server/chroma_db/
server/agent_orchestration/.agent/profiles/*.json
server/__pycache__/
server/**/__pycache__/
server/**/*.pyc
server/.pytest_cache/
server/**/.pytest_cache/
server/logs/*.log
server/logs/*.log.*

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Engine logs
Engine/logs/*.txt

# Scraped HTML caches (generated artefacts — do not commit)
server/web_scraper/knowledge/raw/
finetunning/data/term_cache/*.html
finetunning/data/term_cache/*.htm

# Docker
docker-compose.override.yml

recovered~
recovered_*/
.VSCodeCounter

__pycache__/

# Go build cache (generated by local builds inside module directories)
**/.cache/go-build/

.env
.env.local
.ENV

# Coverage artefacts (refs docs/refactoring/refactoring_guide.md C6)
.coverage
**/.coverage
coverage_html/
**/coverage_html/
.nyc_output/
*.lcov

# Test runner caches
.pytest_cache/
**/.pytest_cache/
.vitest/

# YOLO / Ultralytics training output. Models live in cv/models/; the
# `runs/` tree under repo root is per-run metrics + sample images that
# should not be checked in.
runs/

# Web-scraper local data caches (knowledge raw cache is already
# ignored above; this catches the parallel `data/` tree).
server/web_scraper/data/

# Engine match log dropped by the Rust GameLogger. The Dockerfile-built
# version writes to a volume; the locally-run one writes here.
Engine/Engine/logs/
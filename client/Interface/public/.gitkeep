# Static assets served at the site root (e.g. favicon.ico, robots.txt).
# Empty for now; Next.js requires this directory to exist for the
# Dockerfile's COPY --from=builder /app/public ./public step.

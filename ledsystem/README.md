led_board is the API to interface with from the other subsystems, 
led_server is the flask server to receive HTTP requests from the other subsystems, 
led_system is the file I used while building and testing (it's outdated), 
run_led is the file I use to test the lights with. test.py is to walk though all the leds that should be shown under the board. 
